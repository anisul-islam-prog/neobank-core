# Test configuration for Onboarding module
spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        hbm2ddl:
          create_namespaces: true
        default_schema: schema_onboarding

# Logging
logging:
  level:
    com.neobank.onboarding: DEBUG
    org.testcontainers: INFO
    org.springframework: INFO
