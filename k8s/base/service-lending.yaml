# =============================================================================
# NeoBank Lending — Service (Internal ClusterIP)
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: neobank-lending
  namespace: neobank
  labels:
    app.kubernetes.io/name: neobank-lending
    app.kubernetes.io/component: lending
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8084
      targetPort: 8084
      protocol: TCP
  selector:
    app.kubernetes.io/name: neobank-lending
