# =============================================================================
# NeoBank Cards — Service (Internal ClusterIP)
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: neobank-cards
  namespace: neobank
  labels:
    app.kubernetes.io/name: neobank-cards
    app.kubernetes.io/component: cards
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8085
      targetPort: 8085
      protocol: TCP
  selector:
    app.kubernetes.io/name: neobank-cards
