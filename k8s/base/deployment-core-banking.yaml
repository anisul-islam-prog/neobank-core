# =============================================================================
# NeoBank Core Banking — Deployment
# Core Banking Operations — Port 8083
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: neobank-core-banking
  namespace: neobank
  labels:
    app.kubernetes.io/name: neobank-core-banking
    app.kubernetes.io/component: core-banking
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: neobank-core-banking
  template:
    metadata:
      labels:
        app.kubernetes.io/name: neobank-core-banking
        app.kubernetes.io/component: core-banking
        app.kubernetes.io/part-of: neobank
      annotations:
        kubectl.kubernetes.io/default-container: core-banking
    spec:
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: core-banking
          image: neobank-core-banking:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8083
              protocol: TCP
          envFrom:
            - configMapRef:
                name: neobank-config
            - secretRef:
                name: neobank-secrets
          env:
            - name: SERVER_PORT
              value: "8083"
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 90
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 18
