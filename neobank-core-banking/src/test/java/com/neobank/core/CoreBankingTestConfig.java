package com.neobank.core;

import org.springframework.boot.test.context.TestConfiguration;

/**
 * Minimal test configuration for Core Banking module slice tests.
 * Serves as a marker for test context customization.
 */
@TestConfiguration
public class CoreBankingTestConfig {
    // No beans needed here; this just serves as a marker for test configuration
}
